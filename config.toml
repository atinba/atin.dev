base_url = "https://atin.dev"
compile_sass = true
build_search_index = false

[markdown]
# Theme can be customised by setting the `highlight_theme` variable
# to a theme supported by Zola
highlight_code = true

[extra]

header_nav = [
]

list_pages = false
no_list_date = false

add_toc = true
theme = "light"
